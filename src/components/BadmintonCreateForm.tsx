"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function BadmintonCreateForm() {
  const router = useRouter(); const [name, setName] = useState(""); const [format, setFormat] = useState("knockout"); const [events, setEvents] = useState(["mens-singles"]); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  async function submit(event: React.FormEvent) { event.preventDefault(); setSaving(true); setError(""); const response = await fetch("/api/badminton", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name, format, eventTypes: events, startDate: new Date().toISOString(), endDate: new Date().toISOString(), registrationDeadline: new Date().toISOString(), visibility: "public" }) }); const data = await response.json(); setSaving(false); if (!response.ok) { setError(data.error ?? "Could not create tournament"); return; } router.push(`/badminton/${data.tournament.id}`); }
  return <form onSubmit={submit} className="space-y-4 rounded-2xl border border-white/10 bg-white/[0.03] p-5"><label className="block text-sm text-white/70">Tournament name<input required value={name} onChange={(e) => setName(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-white outline-none" placeholder="City Badminton Open" /></label><label className="block text-sm text-white/70">Format<select value={format} onChange={(e) => setFormat(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-[var(--surface-solid)] px-3 py-2 text-white"><option value="knockout">Knockout</option><option value="round-robin">Round robin</option><option value="group-knockout">Group + knockout</option></select></label><fieldset><legend className="mb-2 text-sm text-white/70">Events</legend><div className="grid gap-2 sm:grid-cols-2">{[["mens-singles", "Men's Singles"], ["womens-singles", "Women's Singles"], ["mens-doubles", "Men's Doubles"], ["womens-doubles", "Women's Doubles"], ["mixed-doubles", "Mixed Doubles"]].map(([value, label]) => <label key={value} className="flex items-center gap-2 text-sm text-white/70"><input type="checkbox" checked={events.includes(value)} onChange={(e) => setEvents((current) => e.target.checked ? [...current, value] : current.filter((item) => item !== value))} />{label}</label>)}</div></fieldset>{error && <p className="text-sm text-rose-300">{error}</p>}<button disabled={saving || events.length === 0} className="rounded-xl bg-emerald-400 px-4 py-2.5 font-semibold text-emerald-950 disabled:opacity-50">{saving ? "Creating..." : "Create badminton tournament"}</button></form>;
}